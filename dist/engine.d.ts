import { AutoDocConfig, DocSection } from './types';
/**
 * Check if an existing output directory contains files NOT generated by docforge.
 * Returns true if there are foreign (non-docforge) files.
 */
export declare function detectFolderConflict(outputDir: string): boolean;
export declare function runAutoDoc(config: AutoDocConfig): Promise<{
    sections: DocSection[];
    healthScore: number;
    outputDir: string;
}>;
//# sourceMappingURL=engine.d.ts.map